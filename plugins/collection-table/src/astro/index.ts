import CollectionTable from "./CollectionTable.astro";

// This export name is required by EmDash's block-component auto-wiring.
export const blockComponents = {
	collection_table: CollectionTable,
};
